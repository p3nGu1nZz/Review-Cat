# ReviewCat environment template
#
# Copy to .env and fill values. Never commit .env.

# --- GitHub auth (used by github-mcp-server when running in stdio mode) ---
GITHUB_PERSONAL_ACCESS_TOKEN=

# Optional: restrict github-mcp-server toolsets (comma-separated)
# Example: issues,pull_requests,repos,git
GITHUB_TOOLSETS=

# --- MCP selection (preferred MVP: remote HTTP MCP) ---
# If you use the remote MCP server, keep this as-is.
MCP_SERVER_URL=https://api.githubcopilot.com/mcp/

# If you use the native stdio github-mcp-server instead of remote HTTP:
# - ensure github-mcp-server is installed and in PATH
# - run Copilot CLI with: --mcp-config dev/mcp/github-mcp-stdio.json
GITHUB_MCP_SERVER_PATH=github-mcp-server

# --- ReviewCat dev harness config ---
# Canonical dev harness config (safe to commit)
REVIEWCAT_DEV_CONFIG=config/dev.toml

# Optional env overrides (planning-level; scripts may honor these)
REVIEWCAT_DIRECTOR_INTERVAL_SECONDS=
REVIEWCAT_MAX_WORKERS=
REVIEWCAT_DOCKER_IMAGE=
REVIEWCAT_DOCKER_WORKDIR=
REVIEWCAT_AGENT_BUS_LISTEN_ADDR=
REVIEWCAT_AGENT_BUS_LISTEN_PORT=
